{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Bullet and App",
            "type": "shell",
            "command": "sh ./build.sh",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}